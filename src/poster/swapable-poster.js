/*
* @Author: gilles
* @Date:   2017-03-27 17:29:36
* @Last Modified by:   gilles
* @Last Modified time: 2017-03-28 08:45:07
*/

'use strict';

// import Swapable from 'nomocas-webutils/lib/swapable';

/*		swapableItem(data, index, ctrl) {
			return this.onDom((node) => {
				ctrl.swapper.add(node, data, index);
			}, null, (node) => {
				ctrl.swapper.remove(node);
			});
		},*/


getInitialState



			// if (props.opt.swapable) {
			// 	this.swapper = new Swapable({
			// 		axis: 'y',
			// 		container: null,
			// 		delegateDragEnd: true
			// 	});
			// 	this.swapper.on('nodeDisplaced', (geom, fromIndex, toIndex) => {
			// 		this.displaceItem(fromIndex, toIndex);
			// 	});
			// 	this.swapper.enabled = props.editMode;
			// }



				// .if(opt.swapable, h.onDom(($tag) => ctrl.swapper.container = $tag))


					// .if(opt.swapable, h.swapableItem(item, index, ctrl))
